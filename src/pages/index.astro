---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const notes = (await getCollection('notes'))
  .filter((note) => !note.data.draft)
  .sort((a, b) => b.data.publishedAt.valueOf() - a.data.publishedAt.valueOf())
  .slice(0, 3);
---

<BaseLayout>
  <section class="front-hero">
    <div class="front-intro">
      <p class="eyebrow">linconvidal.com</p>
      <h1>Software, photography, games, notes.</h1>
      <p class="lede">Things I build, photograph, play with, and write.</p>
    </div>

    <div class="front-list" aria-label="Main sections">
      <a class="front-link" href="/software">
        <span class="front-number">01</span>
        <strong>Software</strong>
        <span>Cobogó, Farol, SpatialMD, tools.</span>
      </a>
      <a class="front-link" href="/photography">
        <span class="front-number">02</span>
        <strong>Photography</strong>
        <span>Essays, zines, selected prints.</span>
      </a>
      <a class="front-link" href="/games">
        <span class="front-number">03</span>
        <strong>Games</strong>
        <span>Pinote, prototypes, devlogs.</span>
      </a>
      <a class="front-link" href="/notes">
        <span class="front-number">04</span>
        <strong>Notes</strong>
        <span>Essays, field notes, fragments.</span>
      </a>
    </div>

    <figure class="front-photo">
      <img src="/photographs/paraty-street.jpg" alt="Street in Paraty photographed by Lincon Vidal" />
      <figcaption>Paraty, 2025</figcaption>
    </figure>
  </section>

  <section class="section notes-section">
    <div>
      <p class="eyebrow">notes / essays</p>
      <h2>Recent writing</h2>
    </div>
    <div class="note-list">
      {notes.map((note) => (
        <a class="note-row" href={`/notes/${note.id}`}>
          <span class="note-date">{note.data.publishedAt.toISOString().slice(0, 10)}</span>
          <span>{note.data.title}</span>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>
